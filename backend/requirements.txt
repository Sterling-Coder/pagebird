PyMuPDF>=1.24
lxml>=5.0
fastapi>=0.110
python-multipart>=0.0.9  # required by FastAPI for UploadFile/Form (/api/translate)
uvicorn>=0.29
pydantic>=2.0
python-dotenv>=1.0
# optional translation engines (pipeline degrades gracefully if absent / no API key)
openai>=1.40
anthropic>=0.39
deepl>=1.18
# optional ingest layers (same contract: absent key -> layer reports and skips)
google-cloud-documentai>=2.20   # ingest/ocr.py — primary OCR
google-cloud-vision>=3.7        # ingest/ocr.py — OCR for stylized/graphic text (banners, callouts)
requests>=2.31                  # protect/equations.py — Mathpix /v3/text
# no-key fallbacks, so OCR and equation recognition still work offline
rapidocr-onnxruntime>=1.3       # ingest/ocr.py — local OCR (CPU, ~10MB models)
albumentations<1.4.0            # pinned to avoid stringzilla compilation issue on Windows without C++ tools
pix2tex>=0.1.4                  # protect/equations.py — local LaTeX OCR (pulls torch)
